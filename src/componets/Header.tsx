import React from "react";

const Header = () => {
  return (
    <header
      className="h-[80px] bg-blue-400 px-2 flex items-center"
      style={{ width: "calc(100vw)" }}
    >
      Header
    </header>
  );
};

export default Header;
